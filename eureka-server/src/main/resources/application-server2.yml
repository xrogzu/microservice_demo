spring:
  application:
    name: eureka-server
server:
  port: 9002
eureka:
  instance:
    hostname: server2
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://server1:9001/eureka,http://server3:9003/eureka

