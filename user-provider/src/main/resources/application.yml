server:
  port: 0 # 随机端口
spring:
  application:
    name: user-provider
  profiles:
    active: dev
  jpa:
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://server1:9001/eureka,http://server2:9002/eureka,http://server3:9003/eureka