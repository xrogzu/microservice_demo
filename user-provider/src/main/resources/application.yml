server:
  port: 0 # 随机端口
  jpa:
    show-sql: true

eureka:
  client:
    healthcheck:
      enabled: true
  instance:
    metadata-map: # 自定义元数据
      author: zhongmingmao
      location: Zhongshan,Guangdong,China

management:
  security:
    enabled: false

info:
  app:
    name: @project.artifactId@
    encoding: @project.build.sourceEncoding@
    java:
      source: @java.version@
      target: @java.version@