server:
  port: 0 # 随机端口
spring:
  application:
    name: user-provider
  profiles:
    active: dev
  jpa:
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:9000/eureka